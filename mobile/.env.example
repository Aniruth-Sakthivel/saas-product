# Mobile app env — copy to .env and fill with the SAME Supabase project as the web app.
# These are the public anon credentials (safe for client apps; RLS protects data).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
